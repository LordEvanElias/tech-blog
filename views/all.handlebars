<div>TechBlog</div>

{{#each posts as |post|}}
<div class='col-md-5'>
    <p>
        {{post.title}}
        {{post.user.username}}
        {{post.body}}
    </p>
</div>
{{!-- <div>
    Posted by {{User.username}} on {{format_date createdAt}}
</div> --}}

{{/each}}